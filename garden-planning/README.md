# ⚠️ Staging directory — these files do not belong in joshify

Planning documents for a **garden planning app**, a separate project from
Joshify. They live here only because this container is ephemeral and the
`JoshHambright/garden` repo does not exist yet — the session's GitHub app
lacks permission to create repositories.

**Next step:** create `JoshHambright/garden` on GitHub, then move this whole
directory to `docs/` in that repo and delete it from here. Nothing in here
should ever be merged into joshify's `main`.

Contents:

| File | Contents |
|---|---|
| `PRODUCT.md` | What we're building, principles, the full feature inventory |
| `DATA_MODEL.md` | The domain model — read this before the roadmap |
| `ROADMAP.md` | Nine phases, each with an exit criterion |
| `DECISIONS.md` | ADR log — the choices already made and why |
